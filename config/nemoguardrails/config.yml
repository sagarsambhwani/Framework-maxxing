models:
  - type: main
    engine: openai
    model: inclusionai/ling-3.0-flash-fin:free
    parameters:
      base_url: https://openrouter.ai/api/v1
      temperature: 0.1

rails:
  input:
    flows:
      - check jailbreak
      - check off topic
      - mask sensitive data
  output:
    flows:
      - check output safety
      - check hallucination
